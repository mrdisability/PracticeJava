type TodoItem {
    id: ID!
    todoTitle: String!
    completed: Boolean
}

type Query {
    allTodos: [TodoItem]
    oneTodo: TodoItem
    todos(isCompleted: Boolean): [TodoItem]
    showTodo(id: String): TodoItem
}